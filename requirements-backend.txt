# Full requirements for backend deployment (Railway/Render/Fly.io)
# Use this file when deploying the FastAPI backend

# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# HTTP Client
requests==2.31.0

# Vector Database & Embeddings
sentence-transformers==2.2.2
chromadb==0.4.18
# Alternative: faiss-cpu==1.7.4  # Uncomment if using FAISS instead of Chroma

# Document Processing
pymupdf==1.23.8  # For PDF parsing
beautifulsoup4==4.12.2
lxml==4.9.3

# LLM Clients (choose based on your provider)
# For Ollama (default)
# No additional package needed, uses requests

# For Groq (optional)
# groq==0.4.0

# For OpenAI (optional)
# openai==1.3.5

# Utilities
python-multipart==0.0.6  # For file uploads in FastAPI

# Testing (optional)
pytest==7.4.3
pytest-asyncio==0.21.1

